//错题分析：
// 错选：D,以为break能够跳出全部循环
//答案是 C。
//
//解释：
//A项错误：内层循环可以使用for、while或do...while中的任何一种。
//B项错误：while和do...while在效率上没有绝对的高低之分，具体取决于使用情境。
//C项正确：三个循环语句在功能上可以互相替代，虽然语法和逻辑结构不同。
//D项错误：break语句只会跳出当前的循环，不会跳出外层循环。
//// 阅读下面的程序，并判断该程序的运行结果是
//
//#include "stdio.h"
//
//main() {
//
//	int k = 17;
//
//	printf("%d,%o,%x\n", k, k, k);
//
//}
// A.
//17, 17, 17
//
//
//B.
//17, 21, 11
//
//
//C.
//17, 021, 0x11
//
//
//D.
//17, 0x11, 021
//选B. 17, 21, 11
//错选：c,以为会输出八进制和十六进制的格式
//解析：
//
//% d用于输出十进制整数，k的值为17，所以输出17。
//% o用于输出八进制整数，17在八进制中是21。
//% x用于输出十六进制整数，17在十六进制中是11。
//因此，运行结果为 17, 21, 11。
// 有定义语句：int x，y，假设要通过scanf(“%d,%d”,&x,&y);  语句  使变量x得到数值11，变量y得到数值12，下面四组输入形式中错误的是
//A.
//11 < 空格 > 12 < 空格 >
//
//
//B.
//11, < 空格>12 < 回车 >
//
//
//C.
//11, 12 < 回车 >
//
//
//D.
//11， < 回车>12 < 回车 >
//选D。在输入中，scanf("%d,%d", &x, &y) 只能正确处理以逗号分隔的两个整数。选项D中有一个换行符在逗号后面，导致无法正确读取第二个整数y，因此是错误的输入形式。
//错选：c纯粹看错了题目
// 
//在一个三角形中，任意两边之和大于第三边。如果变量a、b、c代表三角形的三条边，以下是表示a、b、c能构成一个三角形的C表达式。
//if (a > b + c && b > a + c && c > a + b)
//正确答案：
//a > b + c && b > a + c && c > a + b