//抽象的概念：
//（变量的指针）是用来存储该变量在内存中的地址，通过指针可以访问和修改变量的值。指针本身并不存储值，而是指向存储值的内存位置。
//本题目要求利用fun函数求数组给行的和（填空时不允许出现空格字符）。
//#include "stdio.h"  
//
//void fun(int (*a)[3], int* b); // 形参采用带*的指针变量定义形式  
//
//int main()
//{
//    int a[3][3], i, j, b[3];
//    for (i = 0; i < 3; i++)
//    {
//        for (j = 0; j < 3; j++)
//            scanf_s("%d", &a[i][j]);
//    }
//    fun(a, b);
//    for (i = 0; i < 3; i++)
//    {
//        for (j = 0; j < 3; j++)
//            printf("%4d", a[i][j]);
//        printf("%4d\n", b[i]);
//    }
//    return 0;
//}
//
//void fun(int (*a)[3], int* b)
//{
//    int tmp = 0, i, j;
//    for (i = 0; i < 3; i++)
//    {
//        tmp = 0; // 重置tmp为0  
//        for (j = 0; j < 3; j++)
//        {
//            tmp += a[i][j]; // 使用二维数组的正确访问方式  
//        }
//        b[i] = tmp; // 将行的和存入b  
////    }
//}
//请完善程序，实现以下程序功能：首先从键盘上输入10个不重复的整数保存到数组a中
//，然后，从键盘上输入一个整数进行查找，如果在数组a中找到此数，则输出此数所在的下标，如果找不到此数，则输出“查无此数！”的信息。
//#include <stdio.h>  
//
//int main(void) {
//    int k;
//    for (k = 1; k <= 3; k++) { //该循环用于自动阅卷，请考生忽略  
//        int a[10], data, i, * p, flag = 0;
//        //首先输入10个原始数组元素数据  
//        p = a;
//        for (i = 0; i < 10; i++)
//            scanf("%d", p + i);
//
//        //然后输入一个待查找的数据  
//        scanf("%d", &data);
//        //开始查找  
//        p = a;
//        i = 0;
//        while (i < 10) {
//            if (*(p + i) == data) { //判断当前元素是否等于待查找的数据  
//                flag = 1; //表示找到了  
//                printf("%d\n", i);
//                break; //结束循环  
//            }
//            i++; //移动到下一个元素  
//        }
//        if (flag == 0) //表示没有找到  
//            printf("查无此数！\n");
//    } //该循环用于自动阅卷，请考生忽略  
//    return 0;
//}
//填补以下程序中空白部分的代码，以实现：输入5个字符串，统计字符串中字符‘e’的个数。
#include<stdio.h>  

int main() {
    char a[5][10];
    char* p;
    int cnt = 0; // 要在这里定义cnt，进行计数  

    char (*q)[10] = a; // 指向数组的指针  
    for (q = a; q < a + 5; q++) { // 遍历5个字符串  
        scanf("%s", q); // 读取每个字符串  
        for (p = *q; *p; p++) // 遍历当前字符串中的字符  
            if (*p == 'e') { // 检查字符是否为'e'  
                cnt++; // 如果是'e'，增加计数  
            }
    }
    printf("%d", cnt); // 输出'e'的总数  
    return 0; // 结束程序  
}