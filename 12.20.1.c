//错误1：变量看错了
//#include <stdio.h>
//double fact(double i)
//{
//    double sum = 1;
//    for (int j = 1; j <= i; j++)
//    {
//        sum *= i;//结果是这里错了，找了一个小时还没发现.这里不是i而是j
//    }
//    return sum;
//}
//double com(m, n)
//{
//    if (n < m)
//    {
//        printf("error");
//        return -1;
//    }
//    return fact(n) / ((fact(m) * fact(n - m)));
//}
//int main()
//{
//    double m, n;
//    double result;
//    scanf_s("%lf %lf", &n, &m);
//
//    result = com(m, n);
//    printf("result = %.0lf", result);
//}
////#include <stdio.h>  
////
////// 函数声明  
////double fact(int n);
////
////int main() {
////    int m, n;
////    scanf_s("%d %d", &m, &n); // 输入 m 和 n  
////
////    // 计算组合数 C(n, m) = n! / (m! * (n - m)!)  
////    double combination = fact(n) / (fact(m) * fact(n - m));
////
////    // 输出结果  
////    printf("result = %.0f\n", combination);
////    return 0;
////}
////
////// 计算阶乘的函数  
////double fact(int n) {
////    if (n == 0) return 1; // 0! = 1  
////    double result = 1;
////    for (int i = 1; i <= n; i++) {
////        result *= i; // 逐步计算阶乘  
////    }
////    return result;
////}
//错误二：/----->少写了；
//#include"stdio.h"
//
//main() {
//
//	int i, s;
//
//	i = 1; s = 0;
//
//	while (i < 100) {
//
//		s = s + i;
//		i++ //----->少写了；
//
//	}
//
//	printf("%d\n", s);
//
//}
//错误三：自以为是
//下列程序求Sn = a + aa + aaa-- - aa(n个a)的值, 其中a是一个数字。例如若a = 2, n = 5时, Sn = 2 + 22 + 222 + 2222 + 22222, 其值应为24690。请填空。
//
//#include <stdio.h>
//
//int main(void) {
//    int a, n, count = 1, sn = 0, tn = 0;
//
//    scanf("%d%d", &a, &n);
//    while (count <= n) {
//
//        tn = 2;//自认为知道这里的a就是2，就直接把a换成了2
//        sn = sn + tn;
//        tn *= 10;
//        count++;
//    }
//    printf("%d\n", sn);
//    return 0;
//}
// 关于变量的生存期和作用域，下列说法正确的是：（）
// A.局部变量的作用范围是从定义处到源文件结束。
//B.全局变量作用范围是整个文件。
//C.变量生命周期是指变量从定义开始分配存储单元到运行结束存储单元被回收的整个过程。
//D.若局部变量与全局变量同名，全局变量优先。
//A项错误，局部变量的作用范围是从定义处到其所在的代码块结束，而不是从定义处到源文件结束。
//B项部分正确，但通常全局变量的作用范围是整个文件，若使用了 extern 声明，它可以在多个文件中使用。
//C项正确，变量的生命周期确实是从定义到存储单元被回收的整个过程。
//D项错误，若局部变量与全局变量同名，局部变量会遮蔽全局变量，局部变量优先。
//下列程序的输出结果是（）。
//选D.因为记错了知识点，以为c会被赋值为0
//fun(int a, int b, int c)
//{
//    c = a * b;
//}
//int main(void)
//{
//    int c;
//
//    fun(2, 3, c);
//    printf(“ % d\n”, c);
//
//    return 0;
//}
//
////A.0
////B.1
////C.6
////D.无法确定
//能正确表示a和b同时为正或同时为负的逻辑表达式是_________。//选D看错了
//A.(a >= 0‖b >= 0) && (a < 0‖b < 0)
//B.(a >= 0 && b >= 0) && (a < 0 && b < 0)
//C.(a + b > 0) && (a + b <= 0)
//D.a* b > 0
//*************************（我的知识盲区）
////阅读程序，并判断该程序的运行结果是
////#include "stdio.h"
////void main()
////{
////	int a = 20, b = 10;
////	printf(“ % d, %% d\n”, a + b, a - b);
////}
// 正确输出结果是：30,%d 
////A.
////30, % d
////B.
////30, 10
////C.
////30, % 10
////D.
////以上答案均不正确
////答案是D.以上答案均不正确。
////
////分析程序：
////
////% d 是格式化输出，用于输出整数。
////%% d 会输出一个% 符号，而不是整数。（"%%" 代表一个字面上的百分号 %。在 C 的格式化字符串中，%% 会输出一个 % 字符。"d" 是字面字符，它不会被任何参数替代。
////在 printf 中，% d 是格式化占位符，但%% d 会被解析成直接输出一个% 符号和 d 字符，因此最终输出结果是 30, % d。
////
////所以，正确答案是 D。
//若已定义x和y为double类型，则表达式：x=1,y=x+3/2的值为（）
//A.1
//B.2
//C.2.0
//D.2.5
//写太快了，没注意到问题，就选了D
//正确答案C